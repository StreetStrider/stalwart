#! /usr/bin/env node

var fs = require('fs-sync')

var schema = require(__dirname + '/../schema/schema')

var csst = fs.read(__dirname + '/../schema/css.template')

var render = require('mustache').render

var css = render(csst, schema)

fs.write(__dirname + '/../gen/gen.css', css)
